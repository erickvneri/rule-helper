{
    "name": "If (L == R) -> (wait x time) -> Execute command",
    "actions": [
        {
            "if": {
                "equals": {
                    "left": {},
                    "right": {}
                },
                "then": [
                    {
                        "sleep": {
                            "duration": "{{ integer }}",
                            "unit": "{{ timeUnit }}"
                        }
                    },
                    {
                        "command": {
                            "devices": [],
                            "commands": []
                        }
                    }
                ]
            }
        }
    ]
}